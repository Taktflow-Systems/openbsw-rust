{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/openbsw-rust/controller-fault.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "fixture_id",
    "fixture_revision",
    "board",
    "mode",
    "probe_class",
    "can",
    "artifact",
    "isolation",
    "cases",
    "outcome"
  ],
  "properties": {
    "schema_version": {"const": 1},
    "fixture_id": {"type": "string", "pattern": "^fixture-[a-z0-9]{1,16}$"},
    "fixture_revision": {"const": "hil-fixture-v2"},
    "board": {"enum": ["stm32f413", "stm32g474"]},
    "mode": {"enum": ["busoff", "overflow"]},
    "probe_class": {"const": "st-link"},
    "can": {
      "type": "object",
      "additionalProperties": false,
      "required": ["interface_class", "bitrate", "mode"],
      "properties": {
        "interface_class": {"const": "socketcan"},
        "bitrate": {"const": 500000},
        "mode": {"const": "classic"}
      }
    },
    "artifact": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "sha256"],
      "properties": {
        "name": {"type": "string"},
        "sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"}
      }
    },
    "isolation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["fixture_lock", "peer_isolated", "can_reset", "serial_flushed", "noinit_cleared"],
      "properties": {
        "fixture_lock": {"const": true},
        "peer_isolated": {"const": true},
        "can_reset": {"const": true},
        "serial_flushed": {"const": true},
        "noinit_cleared": {"const": true}
      }
    },
    "cases": {
      "type": "array",
      "minItems": 3,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "passed", "duration_us", "detail", "threshold"],
        "properties": {
          "name": {"type": "string"},
          "passed": {"type": "boolean"},
          "duration_us": {"type": "integer", "minimum": 0},
          "detail": {"type": "string"},
          "threshold": {"type": "string"}
        }
      }
    },
    "outcome": {"enum": ["passed", "failed"]}
  }
}
