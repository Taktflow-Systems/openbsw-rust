# Production unsafe-code inventory

> Generated by `tools/port/inventory_unsafe.py`; site-level classifications are in `unsafe-inventory.json`.

Approved production sites: **392/392**.

Test-only code after a crate's `#[cfg(test)]` module is excluded. Deployable BSP examples are included.

## Ownership summary

| Owner | Sites |
|---|---:|
| `allocator` | 4 |
| `bsp-concurrency` | 3 |
| `bsp-hardware` | 298 |
| `can-posix-ffi` | 18 |
| `concurrency` | 7 |
| `container` | 54 |
| `io-concurrency` | 8 |

## Site-kind summary

| Kind | Sites |
|---|---:|
| `unsafe-block` | 303 |
| `unsafe-extern` | 4 |
| `unsafe-fn` | 74 |
| `unsafe-impl` | 10 |
| `unsafe-token` | 1 |

Every JSON row records its path/line, owner, invariant, caller obligations, target, review set, stable source fingerprint, and verification gates. Every current row is approved. Line numbers are regenerated and must not be used as durable identifiers outside this inventory.
