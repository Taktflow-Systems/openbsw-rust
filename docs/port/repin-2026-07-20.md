# Oracle re-pin record - 2026-07-20

This document records the deliberate, governed move of the pinned upstream
baseline approved by `docs/port/upstream-repin-decision-2026-07-19.md` (U06,
decision "go-with-scope"). The U06 memo is the normative scope document for
this tranche; this record is its execution evidence.

- previous pin: `ddbcf88a62dfcddb1eb07f868ba6412bec1ebf77` (2026-03-12)
- new pin: `be0029bbb79fe901048a24c2665f2ba854328734`
  ("Add STM32 FDCAN transceiver adapter", 2026-06-02)
- re-pin executed: 2026-07-20
- upstream `refs/heads/main` observed live at execution time:
  `be0029bbb79fe901048a24c2665f2ba854328734` - identical to the candidate,
  so **the new recorded drift posture is zero commits** (no extension
  window; the U01 survey method did not need to be re-run).

## 1. Tranche package IDs

Per U06 criterion 7, this tranche carries its own package IDs:

| Package | Scope |
|---|---|
| U07 | Oracle move: checkout, build scripts, `upstream-baseline.md`, pin-reference sweep |
| U08 | Fixture and differential regeneration at the new pin |
| U09 | UDS cluster: 0x14/0x19 composition routing, UdsConfig timing, service dispositions |
| U10 | Transport/DoIP cluster: tester-address translation, send-job sizing, estd-to-ETL re-verification |
| U11 | Middleware/composition cluster: framework contract cross-check, shm-runtime decision, reference-app workflows |
| U12 | STM32 CAN cluster: register-level gaps 33-37 dispositions |
| U13 | Ethernet/lwIP, storage/EEPROM, estd/util/console native-replacement re-reviews |
| U14 | Ledger, status, plan, and closure records |
| U15 | Full verification at the new pin (software scope + HIL boundary statement) |
| U16 | Routing/blob subsystem scoping decision (new parity rows) |

## 2. Gate-zero (pre-move criteria) evidence

All eight U06 criteria were re-verified with fresh evidence on 2026-07-20:

1. U01-U05 docs committed unchanged on `main` (drift-closure change set,
   6 docs + 8 drift-labeled test files).
2. All 36 drift-labeled tests green before any change (13+3+3+6+5+3+2+1
   across the eight suites, exit 0).
3. Oracle checkout and fixture-regeneration plan: this document plus the
   reworked `tools/port/openbsw_oracle.ps1` and new Linux-native
   `tools/port/openbsw_oracle.sh` (pinned SHA `be0029b`, C++17 minimum,
   CMake presets retained under the Bazel-primary layout, middleware
   build-time codegen prerequisites checked). All seven fixture pairs
   regenerate (section 5); regressions are adjudicated against the pinned
   upstream tree and its 1,813-test suite, with divergences recorded as
   parity decisions, never silently absorbed.
4. `upstream-baseline.md` bundled-component/NOTICE table updated per U05
   item 1 in this same change set (ETL 20.48.1, CMSIS path move, ST
   STM32F4 2.6.11 / STM32G4 1.2.6 rows).
5. Upstream tip re-verified live at execution time: exactly `be0029b`.
6. HIL bench: NOT available on the executing host (no probe, no serial
   device, no physical CAN interface). HIL is never skip-capable; the
   tranche therefore stops at the software boundary (section 7) and the
   HIL packages remain open.
7. This dedicated tranche exists with its own package IDs (section 1),
   evidence budget, and the row-reopening list from U06 section 3,
   including the routing/blob new-row decision (section 4, U16).
8. Divergence dispositions decided and recorded per item before the move
   (section 4).

Oracle at the new pin, on this host (see
`docs/test-evidence/samples/repin-2026-07-20-oracle.json`):

- `tools/port/openbsw_oracle.sh Test`: **1,813/1,813 upstream tests pass**
  (previous pin: 1,385).
- `tools/port/openbsw_oracle.sh Reference`: POSIX reference application
  builds and boots at the new pin (lifecycle to run level, CAN demo frames,
  middleware Foo demo cadence visible in the boot log).

## 3. Pin-reference sweep (update-now vs historical-record-keep)

Every repository reference to `ddbcf88` was enumerated and classified.

Updated to `be0029b` (current-pin statements and regenerated artifacts):

- `tools/port/openbsw_oracle.ps1` (+ new `tools/port/openbsw_oracle.sh`)
- `docs/port/upstream-baseline.md` (SHA, date, subject, component table)
- `README.md`
- `docs/port/parity-manifest.json` (`baseline.commit`)
- `docs/port/status.md` (regenerated)
- `docs/port/upstream-test-inventory.md` (regenerated at the new pin)
- `docs/plans/plan-rust-bsw-full-integration.md` (baseline statement plus
  re-pin record)
- `docs/port/release-build.md` (with the historical release note retained)
- the eight parity docs' pinned-upstream header lines (`can`, `docan`,
  `doip`, `estd`, `ethernet`, `reference-app`, `uds`, `util`)
- `crates/openbsw-reference-app/src/scenario.rs` (`oracle_json`
  provenance) and the `f09` provenance assertion in
  `crates/openbsw-reference-app/tests/reference_app.rs`
- all seven live oracle fixture pairs (section 5)
- the promoted baseline test files (former drift tests, section 6)

Kept as historical record (deliberately unchanged):

- `docs/port/release-notes-2026-07-18.md`,
  `docs/port/final-acceptance-2026-07-18.md`,
  `docs/port/safety-mechanisms-2026-07-18.md`, and all
  `docs/test-evidence/samples/*.json` captured against the old pin - the
  evidence record of the completed mandatory release
- the six 2026-07-19 drift docs including the U06 memo (they document the
  `ddbcf88 -> be0029b` window itself)
- `docs/port/upstream-drift-2026-07-18.md`, `docs/port/embedded-doip-2026-07-19.md`
- `docs/port/oracle-fixtures/historical-ddbcf88/` (section 5)
- `.codex-handoff/` session records

No mixed-pin ambiguity remains: every `ddbcf88` occurrence outside this
record is in a file listed above as historical.

## 4. Reopened clusters and dispositions (U06 section 3 table)

Every cluster is either re-closed to parity or carries an explicit recorded
parity decision. Detailed rationale lives in the named parity docs.

| # | U06 cluster | Disposition |
|---|---|---|
| 1 | Service-middleware framework rework | Re-closed. Contract cross-check green at tip (ErrorState 0/4-9, MessageType bits 1/2/4/8/16, MAX_METHOD_ID 128, inline message capacity 64 bytes) via promoted baseline tests; upstream shm-simulation runtime and Foo demo composition wiring declined as recorded native difference (`reference-app-parity.md`) |
| 2 | Routing/blob subsystem, RoutingSystem run level 6 | Explicit exclusion decision: new rows `bsw.routing` and `bsw.blob` added to the manifest as `optional`/`excluded` (package U16) pending a dedicated tranche; mandatory totals unchanged by decision, not by silence |
| 3 | UDS 0x14/0x19 + composition routing + UdsConfig | Re-closed. 0x14/0x19 routed in the reference composition to the existing DEM-backed services (parity with tip `UdsSystem::addDiagJobs`); programming-session P2\*max 5000 (10 ms units) adopted per tip default; service-level demo-vs-DEM divergences kept as recorded native decisions (`uds-parity.md`) |
| 4 | Tester-address translation at TP-router boundaries | Recorded parity decision: full-16-bit logical-address forwarding kept; bounded 2-byte/1-byte translation table remains integrator configuration, pinned by promoted baseline test (`doip-parity.md` transport section) |
| 5 | DoIP estd-to-ETL migration + send-job pool sizing | Re-closed. Fixtures re-verified at the new pin (no wire change; all 20 in-window DoIP commits are ETL migration + copyright headers). Pool sizing (`330514aa` corrects upstream's shared frontend pools to diagnostic=6/protocol=8): recorded structural native difference - Rust `DoIpEntity` has one per-composition const-generic `SEND_JOBS` diagnostic pool (default 6, matching upstream's diagnostic pool) and no separate protocol pool, since protocol responses use per-connection pending-send slots (`doip-parity.md`) |
| 6 | STM32 CAN register-level gaps 33-37 | Deferred with basis, per gap: adoption requires register-level production-driver changes with no host seam and target/HIL re-verification on both boards; no HIL bench in this tranche (`can-parity.md`). These are the STM32-side open items at the software boundary |
| 7 | lwIP netif registry / Tap lifecycle | Re-closed at the observable contract (echo survives reboot/restart, promoted test); netif config registry recorded as not modeled - native difference (`ethernet-parity.md`) |
| 8 | EEPROM-size configurability | Re-closed as config-surface parity; upstream default unchanged; port sizing recorded in `util-parity.md`/storage notes |
| 9 | estd/util removals, StringBufferOutputStream, Timestamp/SystemTimer | Native-replacement re-review re-affirmed at tip; counterpart mappings refreshed where upstream removed estd facilities in favor of ETL (`estd-parity.md`, `util-parity.md`) |
| 10 | POSIX console stdin/UART robustness | Native-replacement re-review re-affirmed (port has its own console implementation) |
| 11 | Toolchain/build/licensing | Re-closed: `upstream-baseline.md` table updated, oracle scripts reworked, SBOM/dependency gates re-run (no crate-graph change, as U05 predicted) |
| 12 | DoCAN | Not functionally reopened (zero source drift); fixture pair re-executed as a side effect of the oracle move |

## 5. Fixture and differential regeneration (U08)

- Retired `ddbcf88` fixture pairs moved unchanged to
  `docs/port/oracle-fixtures/historical-ddbcf88/` (with README). Nothing
  deleted.
- All seven live pairs regenerated/re-verified against the `be0029b`
  oracle build: `can-frame` (bootstrap protocol contract, no pin content),
  `docan` (6 records, byte-unchanged - upstream DoCAN drift is
  license-header-only), `doip-alive` (2), `doip-diagnostic` (3),
  `doip-routing` (1) (all byte-unchanged, representative records
  re-verified against tip sources and tests), `reference-app` (7 records,
  regenerated byte-exact from the application's oracle emitter), `uds`
  (16 records: the original 8 re-verified, plus 8 new 0x14/0x19 vectors
  where the port and tip upstream agree byte-for-byte; divergent
  demo-vs-DEM vectors deliberately excluded and pinned by tests instead).
  Protocol version remains **1** per `oracle-protocol.md` (no
  record-shape change, so no version bump); `upstream_commit` provenance
  now `be0029b` in every pinned pair.
- `tools/port/compare_oracle.py` passes on all seven pairs (run in the
  full verification, section 7).

## 6. Drift-test promotion (former drift tests are baseline behavior)

The eight 2026-07-19 drift-labeled test files were promoted into normal
parity evidence, none deleted, no assertion weakened:

- `crates/bsw-uds/tests/drift_be0029b.rs` -> `baseline_be0029b.rs`
- `crates/bsw-doip/tests/drift_be0029b.rs` -> `baseline_be0029b.rs`
- `crates/bsw-transport/tests/drift_be0029b.rs` -> `baseline_be0029b.rs`
- `crates/bsw-can/tests/drift_stm32_can_semantics.rs` -> `baseline_stm32_can_semantics.rs`
- `crates/bsw-bsp-stm32/tests/drift_stm32_can_semantics.rs` -> `baseline_stm32_can_semantics.rs`
- `crates/bsw-middleware/tests/middleware_drift_2026_07_19.rs` -> `baseline_be0029b.rs`
- `crates/openbsw-reference-app/tests/reference_app_drift_2026_07_19.rs` -> `baseline_be0029b.rs`
- `crates/bsw-reference-core/tests/composition_drift_2026_07_19.rs` -> `baseline_be0029b.rs`

Headers relabeled from drift observation to pinned-baseline evidence;
upstream-behavior-change records folded to plain baseline behavior;
deliberate-native-difference records folded to recorded parity decisions
citing the parity docs.

## 7. Verification at the new pin and the software boundary

Software-scope verification per `docs/port/test-guide.md` (results recorded
in the tranche handoff): fmt, workspace tests (all features, locked), the
pinned-container workspace run, clippy `-D warnings`, all policy checks
(no_std, features, dependencies, evidence, unsafe inventory, privacy,
status/SBOM freshness), cargo-deny, the 14 fuzz targets at preserved
counts, STM32 library/example checks, and the seven oracle comparisons
against the `be0029b` oracle.

Resource baselines: re-affirmed, not re-measured - no dependency, profile,
or production-code change in this tranche alters the release-build recipe
beyond the additions recorded here, and `measure_resources.ps1` is bound to
the Windows release host. Re-measurement rides with the next release build.

**Open at the software boundary (HIL is never skip-capable):**

- The full HIL physical matrix on both boards
  (`run_physical_matrix.ps1` scope: smokes, destructive storage, queue
  overflow, bus-off, ten resets, ten-minute soak) - package U15 tail,
  covering the G-row physical evidence at the new pin.
- STM32 CAN gaps 33-37 adoption decisions remain deferred (package U12)
  until a bench exists; the deferral records in `can-parity.md` are the
  interim closure.
- The Windows-host release pipeline steps
  (`create_clean_build_roots.ps1`, `build_release_candidate.ps1`,
  `build_posix_reproducible.ps1`, `assemble_hil_artifacts.ps1`,
  `measure_resources.ps1` re-measurement) ride with the next release
  build; they are release-gate scope, not re-pin scope.

## 8. Ledger effect

All total changes below are explicit recorded decisions, none silent:

- Mandatory rows: **37/37 -> 38/38 closed**. The one addition is
  `bsw.time` (upstream extracted `TimestampProvider` into the new
  `libs/bsw/time` module at the tip, commit `9d3e89a2`); it is closed
  immediately because the existing native `bsw-time` port covers it, with
  the injected-time contract pinned by the promoted
  `bsw-reference-core` baseline test. Package-hours remain 1,092/1,092:
  no new mandatory implementation work was created by the row.
- `bsw.estd` row: `upstream_path` repointed from the removed
  `libs/bsw/estd` (deleted upstream at the tip in favor of bundled ETL)
  to `libs/3rdparty/etl`; the native replacement is unaffected and the
  counterpart mapping refresh is recorded in `estd-parity.md`.
- New rows `bsw.routing` and `bsw.blob`: `optional`/`excluded` by
  explicit decision (section 4, cluster 2); they do not enter the
  mandatory totals.
- The completed 2026-07-18 mandatory release and all its evidence remain
  the valid historical record at `ddbcf88`.
